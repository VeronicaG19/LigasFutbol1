import 'package:flutter/material.dart';

class TransferPlayerPage extends StatelessWidget {
  const TransferPlayerPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
