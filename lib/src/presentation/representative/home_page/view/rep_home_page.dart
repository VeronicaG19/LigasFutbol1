import 'package:flutter/material.dart';

class RepresentativeHomePage extends StatelessWidget {
  const RepresentativeHomePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
